name: Build offline dependency-aware Phase 3

on:
  push:
    branches:
      - feature/offline-dependent-setup-phase3

permissions:
  contents: write

jobs:
  build:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/offline-dependent-setup-phase3
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Apply Phase 3
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path

          def replace(path, old, new):
              p = Path(path)
              text = p.read_text()
              if old not in text:
                  raise SystemExit(f'Expected source not found in {path}: {old[:120]!r}')
              p.write_text(text.replace(old, new, 1))

          replace('shared/offline-setup.ts',
          "export type OfflineSetupInput = (OfflineSetupDraft & {\n  offlineOperation: 'setup_create';\n  /** The durable outbox id. It is also the idempotency key for server replay. */\n  clientRef?: string | null;\n});",
          "export type OfflineSetupInput = (OfflineSetupDraft & {\n  offlineOperation: 'setup_create';\n  /** The durable outbox id. It is also the idempotency key for server replay. */\n  clientRef?: string | null;\n  /** Durable outbox ids that must be acknowledged before this setup row can sync. */\n  offlineDependsOn?: string[];\n});")

          replace('shared/offline-setup.ts',
          "export function offlineSetupReceiptId(clientRef: string | null | undefined): string {\n  return `rcp_${token(clientRef)}`;\n}\n",
          "export function offlineSetupReceiptId(clientRef: string | null | undefined): string {\n  return `rcp_${token(clientRef)}`;\n}\n\n/** Entity id this setup row requires to exist first, if any. */\nexport function offlineSetupParentEntityId(input: OfflineSetupDraft | OfflineSetupInput): string | null {\n  if (input.setupType === 'business') return null;\n  if (input.setupType === 'account') return input.businessId ?? null;\n  if (input.setupType === 'project' || input.setupType === 'person') return input.businessId;\n  return input.accountId ?? null;\n}\n")

          Path('client/src/offline-dependencies.ts').write_text(r'''import type { Queued } from './offline-db';
import { isOfflineSetupInput, type OfflineSetupInput } from '../../shared/offline-setup';

export function queuedDependencies(item: Queued): string[] {
  if (!isOfflineSetupInput(item.input as unknown)) return [];
  const deps = (item.input as unknown as OfflineSetupInput).offlineDependsOn;
  return Array.isArray(deps) ? [...new Set(deps.filter((id) => typeof id === 'string' && !!id))] : [];
}

/**
 * Stable topological ordering for the outbox. Unrelated writes keep their
 * original relative order, while an explicitly named dependency always moves
 * ahead of its child even after IndexedDB reload/timestamp ties.
 */
export function orderQueuedByDependencies<T extends Queued>(queue: T[]): T[] {
  if (queue.length < 2) return [...queue];
  const pending = [...queue];
  const queueIds = new Set(pending.map((item) => item.id));
  const completed = new Set<string>();
  const ordered: T[] = [];

  while (pending.length) {
    const ready = pending.findIndex((item) =>
      queuedDependencies(item).every((dependency) => !queueIds.has(dependency) || completed.has(dependency)));
    if (ready < 0) {
      throw new Error('Offline setup dependency cycle detected. Review the queued setup changes before syncing.');
    }
    const [item] = pending.splice(ready, 1);
    ordered.push(item);
    completed.add(item.id);
  }
  return ordered;
}

export function queuedDependents(queue: Queued[], parentId: string): Queued[] {
  return queue.filter((item) => queuedDependencies(item).includes(parentId));
}
''')

          replace('client/src/offline.ts',
          "import { isOfflineSetupInput, offlineSetupEntityId, type OfflineSetupDraft, type OfflineSetupInput } from '../../shared/offline-setup';",
          "import { isOfflineSetupInput, offlineSetupEntityId, offlineSetupParentEntityId, type OfflineSetupDraft, type OfflineSetupInput } from '../../shared/offline-setup';\nimport { orderQueuedByDependencies, queuedDependents } from './offline-dependencies';")

          old = '''function setupParentConfirmed(draft: OfflineSetupDraft, confirmed: LoadedBook): boolean {
  if (draft.setupType === 'business') return true;
  if (draft.setupType === 'account') return !draft.businessId || confirmed.businesses.some((item) => item.id === draft.businessId);
  if (draft.setupType === 'project' || draft.setupType === 'person') return confirmed.businesses.some((item) => item.id === draft.businessId);
  return !draft.accountId || confirmed.accounts.some((item) => item.id === draft.accountId);
}

async function queueSetup(draft: OfflineSetupDraft): Promise<Queued> {
  await syncActivation;
  const confirmed = snapshot.loadConfirmed<LoadedBook>();
  if (!confirmed) throw new Error('Load the book once online before changing setup offline.');
  if (!setupParentConfirmed(draft, confirmed)) {
    throw new Error('That setup item depends on another unsynced setup change. Let the parent sync first; chained offline setup is added in the next phase.');
  }
  const prepared = { ...draft, offlineOperation: 'setup_create' } as OfflineSetupInput;
  const item = await offlineRepository.queueAdd(prepared as unknown as EntryInput);
  await offlineSyncState.registerQueued(item.id);
  return offlineSyncState.effective([item])[0] ?? item;
}'''
          new = '''function confirmedSetupParent(draft: OfflineSetupDraft, confirmed: LoadedBook, parentId: string): boolean {
  if (draft.setupType === 'account' || draft.setupType === 'project' || draft.setupType === 'person') {
    return confirmed.businesses.some((item) => item.id === parentId);
  }
  if (draft.setupType === 'reminder') return confirmed.accounts.some((item) => item.id === parentId);
  return true;
}

function setupDependencies(draft: OfflineSetupDraft, confirmed: LoadedBook): string[] {
  const parentId = offlineSetupParentEntityId(draft);
  if (!parentId || confirmedSetupParent(draft, confirmed, parentId)) return [];

  const pending = offlineSyncState.effective(offlineRepository.queueAll());
  const parent = pending.find((item) => {
    if (!isOfflineSetupInput(item.input as unknown)) return false;
    const setup = item.input as unknown as OfflineSetupInput;
    if (offlineSetupEntityId(setup) !== parentId) return false;
    if (draft.setupType === 'reminder') return setup.setupType === 'account';
    return setup.setupType === 'business';
  });
  if (!parent) throw new Error('That setup item points to a parent that is not available offline. Reload once online or choose an existing queued parent.');

  const state = offlineSyncState.stateFor(parent.id);
  if (state.status === 'conflict' || state.status === 'rejected') {
    throw new Error('That parent setup change needs review before you can add more offline changes under it.');
  }
  return [parent.id];
}

async function queueSetup(draft: OfflineSetupDraft): Promise<Queued> {
  await syncActivation;
  const confirmed = snapshot.loadConfirmed<LoadedBook>();
  if (!confirmed) throw new Error('Load the book once online before changing setup offline.');
  const offlineDependsOn = setupDependencies(draft, confirmed);
  const prepared = { ...draft, offlineOperation: 'setup_create', offlineDependsOn } as OfflineSetupInput;
  const item = await offlineRepository.queueAdd(prepared as unknown as EntryInput);
  await offlineSyncState.registerQueued(item.id);
  return offlineSyncState.effective([item])[0] ?? item;
}'''
          replace('client/src/offline.ts', old, new)

          replace('client/src/offline.ts',
          "  drop: async (id: string): Promise<void> => {\n    await offlineRepository.queueDrop(id);\n    await offlineSyncState.remove(id);\n  },",
          "  drop: async (id: string): Promise<void> => {\n    const dependents = queuedDependents(offlineRepository.queueAll(), id);\n    if (dependents.length) {\n      throw new Error(`This queued setup change still has ${dependents.length} dependent ${dependents.length === 1 ? 'change' : 'changes'}. Discard the dependent change first.`);\n    }\n    await offlineRepository.queueDrop(id);\n    await offlineSyncState.remove(id);\n  },")

          replace('client/src/offline.ts',
          "  const rawQueue = offlineSyncState.effective(offlineRepository.queueAll());",
          "  const rawQueue = orderQueuedByDependencies(offlineSyncState.effective(offlineRepository.queueAll()));")

          replace('client/src/offline-projection.ts',
          "import type { Queued } from './offline-db';",
          "import type { Queued } from './offline-db';\nimport { orderQueuedByDependencies } from './offline-dependencies';")
          replace('client/src/offline-projection.ts',
          "  for (const item of [...projectable].sort((a, b) => a.queuedAt.localeCompare(b.queuedAt) || a.id.localeCompare(b.id))) {",
          "  for (const item of orderQueuedByDependencies(projectable)) {")

          replace('client/src/views/Setup.tsx',
          "  const confirmedBusinesses = book.businesses.filter((item) => !pendingIds.has(item.id));\n  const confirmedAccounts = book.accounts.filter((item) => !pendingIds.has(item.id));\n  const first = confirmedBusinesses[0]?.id ?? '';",
          "  const confirmedBusinesses = book.businesses.filter((item) => !pendingIds.has(item.id));\n  const availableBusinesses = book.businesses;\n  const availableAccounts = book.accounts;\n  const first = availableBusinesses[0]?.id ?? '';\n  const firstConfirmed = confirmedBusinesses[0]?.id ?? '';")

          replace('client/src/views/Setup.tsx',
          "          <div className=\"f\"><label>Opening balance</label><input inputMode=\"decimal\" value={acc.opening} onChange={(e) => setAcc({ ...acc, opening: e.target.value })} placeholder=\"0\" /></div>\n          <button className=\"btn\" disabled={!acc.name.trim()} onClick={() => {\n            void keepSetup(() => outbox.setup({ setupType: 'account', name: acc.name.trim(), businessId: null, opening: Number(acc.opening) || 0 }), 'Account created.');",
          "          <div className=\"f\"><label>Business</label><select value={acc.businessId} onChange={(e) => setAcc({ ...acc, businessId: e.target.value })}><option value=\"\">Shared / no business</option>{availableBusinesses.map((b) => <option key={b.id} value={b.id}>{b.name}{pendingIds.has(b.id) ? ' · pending sync' : ''}</option>)}</select></div>\n          <div className=\"f\"><label>Opening balance</label><input inputMode=\"decimal\" value={acc.opening} onChange={(e) => setAcc({ ...acc, opening: e.target.value })} placeholder=\"0\" /></div>\n          <button className=\"btn\" disabled={!acc.name.trim()} onClick={() => {\n            void keepSetup(() => outbox.setup({ setupType: 'account', name: acc.name.trim(), businessId: acc.businessId || null, opening: Number(acc.opening) || 0 }), 'Account created.');")

          replace('client/src/views/Setup.tsx',
          "{confirmedBusinesses.map((b) => <option key={b.id} value={b.id}>{b.name}</option>)}",
          "{availableBusinesses.map((b) => <option key={b.id} value={b.id}>{b.name}{pendingIds.has(b.id) ? ' · pending sync' : ''}</option>)}")
          replace('client/src/views/Setup.tsx',
          "<button className=\"btn\" disabled={!prj.name.trim() || !confirmedBusinesses.length}",
          "<button className=\"btn\" disabled={!prj.name.trim() || !availableBusinesses.length}")

          # second business select for People
          replace('client/src/views/Setup.tsx',
          "{confirmedBusinesses.map((b) => <option key={b.id} value={b.id}>{b.name}</option>)}",
          "{availableBusinesses.map((b) => <option key={b.id} value={b.id}>{b.name}{pendingIds.has(b.id) ? ' · pending sync' : ''}</option>)}")
          replace('client/src/views/Setup.tsx',
          "<button className=\"btn\" disabled={!per.name.trim() || !confirmedBusinesses.length}",
          "<button className=\"btn\" disabled={!per.name.trim() || !availableBusinesses.length}")

          replace('client/src/views/Setup.tsx',
          "{confirmedAccounts.map((a) => <option key={a.id} value={a.id}>{a.name}</option>)}",
          "{availableAccounts.map((a) => <option key={a.id} value={a.id}>{a.name}{pendingIds.has(a.id) ? ' · pending sync' : ''}</option>)}")

          replace('client/src/views/Setup.tsx',
          "      {book.businesses.length > 1 && (",
          "      {confirmedBusinesses.length > 1 && (")
          replace('client/src/views/Setup.tsx',
          "<select value={loan.fromBusiness || first} onChange={(e) => setLoan({ ...loan, fromBusiness: e.target.value })}>{book.businesses.map((b) => <option key={b.id} value={b.id}>{b.name}</option>)}</select>",
          "<select value={loan.fromBusiness || firstConfirmed} onChange={(e) => setLoan({ ...loan, fromBusiness: e.target.value })}>{confirmedBusinesses.map((b) => <option key={b.id} value={b.id}>{b.name}</option>)}</select>")
          replace('client/src/views/Setup.tsx',
          "<option value=\"\">Choose business</option>{book.businesses.map((b) => <option key={b.id} value={b.id}>{b.name}</option>)}</select>",
          "<option value=\"\">Choose business</option>{confirmedBusinesses.map((b) => <option key={b.id} value={b.id}>{b.name}</option>)}</select>")
          replace('client/src/views/Setup.tsx',
          "disabled={!loan.toBusiness || loan.toBusiness === (loan.fromBusiness || first)}",
          "disabled={!loan.toBusiness || loan.toBusiness === (loan.fromBusiness || firstConfirmed)}")
          replace('client/src/views/Setup.tsx',
          "fromBusiness: loan.fromBusiness || first,",
          "fromBusiness: loan.fromBusiness || firstConfirmed,")

          Path('client/src/offline-safe-setup-phase3.test.ts').write_text(r'''import { afterEach, describe, expect, it } from 'vitest';
import type { LoadedBook } from './api';
import { initializeOfflineStorage, lastUser, outbox, resetOfflineStorageForTests, snapshot } from './offline';
import { orderQueuedByDependencies } from './offline-dependencies';
import { offlineSetupEntityId, type OfflineSetupInput } from '../../shared/offline-setup';
import type { Queued } from './offline-db';

function baseBook(): LoadedBook {
  return {
    businesses: [{ id: 'biz_confirmed', name: 'Confirmed' }],
    accounts: [{ id: 'acc_confirmed', name: 'Cash', businessId: 'biz_confirmed', opening: 100 }],
    projects: [], receipts: [], people: [], loans: [], entries: [], reminders: [],
    balances: { totalCash: 100, accounts: { acc_confirmed: 100 }, businesses: { biz_confirmed: 100 }, people: {}, loans: {}, projects: {} },
  };
}

async function ready(): Promise<void> {
  await resetOfflineStorageForTests();
  await initializeOfflineStorage();
  await lastUser.save({ id: 'owner-phase3', role: 'owner' });
  await snapshot.save(baseBook());
}

afterEach(async () => { await resetOfflineStorageForTests(); });

describe('Offline safe setup Phase 3 dependencies', () => {
  it('queues a full parent-child chain using projected final ids', async () => {
    await ready();
    const business = await outbox.setup({ setupType: 'business', name: 'Offline Business' });
    const businessInput = business.input as unknown as OfflineSetupInput;
    const businessId = offlineSetupEntityId(businessInput);

    const account = await outbox.setup({ setupType: 'account', name: 'Offline Cash', businessId, opening: 25 });
    const accountInput = account.input as unknown as OfflineSetupInput;
    const accountId = offlineSetupEntityId(accountInput);
    const project = await outbox.setup({ setupType: 'project', name: 'Offline Job', businessId, opening: 300, scope: '' });
    const person = await outbox.setup({ setupType: 'person', name: 'Offline Supplier', businessId, kind: 'payable', opening: 75, salary: 0, role: 'Supplier' });
    const reminder = await outbox.setup({ setupType: 'reminder', what: 'Offline reminder', amount: 9, accountId, note: '' });

    expect((account.input as any).offlineDependsOn).toEqual([business.id]);
    expect((project.input as any).offlineDependsOn).toEqual([business.id]);
    expect((person.input as any).offlineDependsOn).toEqual([business.id]);
    expect((reminder.input as any).offlineDependsOn).toEqual([account.id]);

    const projected = snapshot.load<LoadedBook>()!;
    expect(projected.businesses.some((item) => item.id === businessId)).toBe(true);
    expect(projected.accounts.find((item) => item.id === accountId)?.businessId).toBe(businessId);
    expect(projected.balances.businesses[businessId]).toBe(25);
    expect(projected.balances.accounts[accountId]).toBe(25);
    expect(projected.balances.totalCash).toBe(125);
  });

  it('topologically restores parent-before-child order after persistence ordering changes', () => {
    const parent: Queued = {
      id: 'q_parent', queuedAt: '2026-09-06T11:20:00.000Z',
      input: { offlineOperation: 'setup_create', setupType: 'business', name: 'Parent', clientRef: 'q_parent', offlineDependsOn: [] } as any,
    };
    const child: Queued = {
      id: 'q_child', queuedAt: '2026-09-06T11:20:00.000Z',
      input: { offlineOperation: 'setup_create', setupType: 'account', name: 'Child', businessId: offlineSetupEntityId(parent.input as any), opening: 0, clientRef: 'q_child', offlineDependsOn: ['q_parent'] } as any,
    };
    expect(orderQueuedByDependencies([child, parent]).map((item) => item.id)).toEqual(['q_parent', 'q_child']);
  });

  it('does not allow a parent to be discarded while children depend on it', async () => {
    await ready();
    const business = await outbox.setup({ setupType: 'business', name: 'Offline Business' });
    const businessId = offlineSetupEntityId(business.input as any);
    await outbox.setup({ setupType: 'project', name: 'Child Project', businessId, opening: 0, scope: '' });
    await expect(outbox.drop(business.id)).rejects.toThrow(/dependent change/);
  });

  it('still refuses references that are neither confirmed nor durably queued', async () => {
    await ready();
    await expect(outbox.setup({ setupType: 'project', name: 'Orphan', businessId: 'biz_missing', opening: 0, scope: '' }))
      .rejects.toThrow(/not available offline/);
  });
});
''')
          PY
      - name: Verify Phase 3
        run: npm run typecheck && npm test -- --passWithNoTests && npm run build
      - name: Commit Phase 3 implementation
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add shared/offline-setup.ts client/src/offline-dependencies.ts client/src/offline.ts client/src/offline-projection.ts client/src/views/Setup.tsx client/src/offline-safe-setup-phase3.test.ts
          git commit -m "Phase 3: sync dependent offline setup safely"
          git push origin HEAD:feature/offline-dependent-setup-phase3
