name: Accounting Chaos Certification

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: accounting-chaos-${{ github.ref }}
  cancel-in-progress: true

jobs:
  certify-accounting-chaos:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: book
          POSTGRES_PASSWORD: book
          POSTGRES_DB: book_phase5_chaos
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U book -d book_phase5_chaos"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    env:
      TEST_DATABASE_URL: postgres://book:book@127.0.0.1:5432/book_phase5_chaos
      DATABASE_URL: postgres://book:book@127.0.0.1:5432/book_phase5_chaos
      PGSSL: off
      SESSION_SECRET: phase5-ci-${{ github.sha }}-session-secret-value
      NODE_ENV: test
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Prepare accounting chaos schema
        run: npm run db:migrate
      - name: Run Phase 5 accounting chaos certification
        run: npm run test:phase5-chaos
